<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:uses-permissionandroid="http://schemas.android.com/apk/distribution"
    package="com.example.s328084s333761mappe2">
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SEND_SMS"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SettPreferanser" android:parentActivityName=".MainActivity">

        </activity>
        <activity android:name=".VisMoteActivity" android:parentActivityName=".VisMoteActivity">

        </activity>
        <activity android:name=".KontaktActivity" android:parentActivityName=".MainActivity">

        </activity>
        <activity android:name=".MøteActivity" android:parentActivityName=".MainActivity"></activity>

        <receiver android:name=".MinBroadcastReceiver">
            <intent-filter>
                <action android:name="com.example.mappe2.mittbroadcast" />
            </intent-filter>
        </receiver>
        <service android:name=".MinService" />
        <service android:name=".SettPeriodiskService" />

    </application>

</manifest>